import { Routes } from '@angular/router';
import {ModelConfiguration} from './model-configuration/model-configuration';

export const routes: Routes = [
  {path: "", component: ModelConfiguration}
];
